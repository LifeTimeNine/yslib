default = file
close = off
channels.file.type = File
channels.file.path = ROOT_PATH'runtime/log'
channels.file.single = off
channels.file.max_files = 0
channels.file.json = off
channels.file.close = off
channels.file.format = '[%s][%s] %s'
channels.file.realtime_write = off